use('commerce');
db.produtos.find(
  { $where: 'this.curtidas > this.vendidos'},
  {_id: 0, nome: 1, curtidas: 1, vendidos: 1 }
);

use('commerce');
db.produtos.find(
  { $expr: { $gt: ["$curtidas", "$vendidos"] } },
  { _id: 0, nome: 1, curtidas: 1, vendidos: 1 },
);